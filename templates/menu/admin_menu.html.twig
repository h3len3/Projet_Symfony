{# version public_menu :

<nav class="navbar navbar-dark main-nav">

    <span class="float-left">
        <a href="#" class="btn btn-nav">Contact</a>
    </span>

    <span class="float-right">
            
        {% if app.user %}
            <a href="{{ path("admin_index") }}" class="btn btn-nav">Admin</a>
            <a href="{{ path("app_logout") }}" class="btn btn-nav">Déconnexion</a>
        {% else %}
            <a href="{{ path("app_login") }}" class="btn btn-nav">Espace pro</a>
        {% endif %}
    </span>

</nav> #}



<nav class="navbar navbar-dark admin-nav">
        <span class="float-left">
                <a href="{{ path("admin_index") }}" class="btn btn-nav">Home</a>
                <a href="{{ path("admin_liste_employes") }}" class="btn btn-nav">Liste employés</a>
                <a href="{{ path("admin_liste_ordinateurs") }}" class="btn btn-nav">Liste ordinateurs</a>
        </span>
        <span class="float-right">
                <a href="{{ path("accueil") }}" class="btn btn-nav">Retour sur le site</a>
                <a href="{{ path("app_logout") }}" class="btn btn-nav">Déconnexion</a>
        </span>
</nav>